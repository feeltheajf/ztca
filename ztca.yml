log:
  level: debug
  format: text

api:
  http:
    address: 127.0.0.1:9443
  auth: 
    apiToken: ${ZTCA_API_TOKEN}

db:
  url: ztca.sqlite3
  driver: sqlite3
  automigrate: yes

pki:
  ca:
    certificate: testdata/pki/ca.crt
    privateKey: testdata/pki/ca.key